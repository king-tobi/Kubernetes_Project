kind: Service                             
apiVersion: v1
metadata:
  name: TobiDevops
  labels:
    app: TobiDevops
spec:
  ports:
    - port: 8080                               
      targetPort: 80                    
      nodePort: 31200
  selector:
    app: TobiDevops                    
  type: LoadBalancer                   
